# Run

Full walkthrough: **`CLEVER_STARTER.md`**. Short form (Rancher Running, venv on, `.env` filled):

```powershell
powershell -File scripts\dev.ps1
```

Then http://127.0.0.1:8080/ (chat), `/dashboard`, `/docs`.
